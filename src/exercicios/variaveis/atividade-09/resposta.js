// declaramos a nota do aluno
let notaDoAluno = 3
// guardamos o resultado da comparação em uma variável
// essa comparacao vai retornar true ou false
let foiAprovado = (notaDoAluno >= 7)

// imprime o resultado
console.log(`O aluno foi aprovado? ${foiAprovado}`)